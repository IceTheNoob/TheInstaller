echo "welcome to the every thing you need to start out with when installing the ubuntu on chronos RUN AS ROOT"
echo "wait 4 seconds for bundle to install"
echo "Did you install linux? to tired to install all of this? well use the bundle"
echo "Note: Some might not work"
sleep 4
apt-get install software-center
apt-get install firefox
apt-get install adobe-flashplugin
apt-get update
apt-get upgrade
apt-get nano

